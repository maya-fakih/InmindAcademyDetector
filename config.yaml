data:
  raw_dir: dataset

model:
  checkpoint: yolo26n.pt

train:
  epochs: 20
  batch_size: 4
  learning_rate: 0.005
  momentum: 0.9
  weight_decay: 0.0005
  num_workers: 0
  seed: 0

output_dir: runs/baseline

wandb:
  enabled: false
  project: inmind-loco-detection
  entity: null
